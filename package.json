{
  "name": "swaprika",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "What to use instead of an ingredient, and where the swap works. A static site.",
  "scripts": {
    "setup": "npm install && npm --prefix web install",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.136.2"
  }
}
